# Schema for Guild membership credential generation
This repo is used by Guild's Privado/Polygon issuer node.

For more information on schema generation, check out [these resources](https://tools.privado.id/).
